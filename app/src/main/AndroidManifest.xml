<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mysto.rickmortybuddyapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.activeandroid.app.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_custom"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_custom"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Episode_Details_Activity" />
        <activity android:name=".Location_Details_Activity" />
        <activity android:name=".Personnage_Details_Activity"></activity>


        // Meta for Active Android
        <meta-data android:name="AA_DB_NAME" android:value="Rick_N_Morty_Buddy_DB" />
        <meta-data android:name="AA_DB_VERSION" android:value="5" />

    </application>


</manifest>